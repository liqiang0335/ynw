////////////////////////////////////
/**
 * 配置文件
 */
const config = {
  alias: {},
  devServer: {},
  keys: {
    index: {
      entry: "./example/index",
      publicPath: "/dist/"
    }
  }
};
////////////////////////////////////
////////////////////////////////////
const build = require("./webpack");
build(config);
////////////////////////////////////
////////////////////////////////////
