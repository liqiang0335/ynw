/**
 * ----------------------------------------
 * 数字填充的数组
 * ----------------------------------------
 */
function numberArray(length) {
  return Array.from({ length }, (_, i) => i);
}

module.exports = numberArray;
